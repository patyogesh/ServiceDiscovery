version : '2'
services :
  mongodb :
    build :
      context : .
      dockerfile : MongoDockerfile
    environment :
      - DB_PATH=${DB_PATH}
    volumes :
      - ${DB_PATH}:${DB_PATH}
    hostname : mongodb